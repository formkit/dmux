#!/bin/bash
set -e
source "$DMUX_ROOT/.dmux-hooks/lib/attentive.sh"

cd "$DMUX_WORKTREE_PATH"

make githooks

for f in .env.local .env.development; do
  [ -f "$DMUX_ROOT/$f" ] && cp "$DMUX_ROOT/$f" "$DMUX_WORKTREE_PATH/$f"
done

echo "[Hook] Worktree ready: $DMUX_SLUG"
